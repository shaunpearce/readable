import React from "react";

const AddIcon = props => (
  <svg viewBox="0 0 28 57" {...props} style={{fill: 'currentcolor'}}>
    <text
      transform="translate(0 45)"
      fontSize={48}
      fontFamily="Roboto-Regular,Roboto"
      letterSpacing=".05em"
    >
      <tspan x={0} y={0}>
        +
      </tspan>
    </text>
  </svg>
);

export default AddIcon

